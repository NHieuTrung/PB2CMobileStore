@model IEnumerable<Models.EF.BrandType>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BrandName)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BrandName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.BrandTypeId }) |
                @Html.ActionLink("Details", "Details", new { id=item.BrandTypeId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.BrandTypeId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
